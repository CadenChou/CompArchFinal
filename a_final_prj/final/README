Setup:
- Install RISC-V compiler
  - Run the following:
    - sudo apt update
    - sudo apt install gcc-riscv64-linux-gnu g++-riscv64-linux-gnu
  
  


How to run
- cd to *final_prj/final directory
- compile using: make for non-MT or make MODE=mt for m
- cd to gem5 directory
- run using: build/RISCV/gem5.debug configs/deprecated/example/se.py   -c ./*final_prj/final/transformer_run.riscv
  - or: 
  build/RISCV/gem5.debug configs/deprecated/example/se.py \
  -c ./\*final_prj/final/transformer_run.riscv \
  --cpu-type=TimingSimpleCPU \
  --num-cpus=4 \
  --caches --l2cache \
  --mem-size=512MB
